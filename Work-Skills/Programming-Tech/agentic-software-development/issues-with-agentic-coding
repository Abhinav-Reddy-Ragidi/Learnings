Frontend

Frontend the issues are until and unless we have a proper design system - claude code writes new components everytime and without following proper structure - which results in a very bad codebase\
Where someone who is working newly cannot understand very easily - this is complex code as per outster hout principles. So its better to design some workflow for developing anything which everyones\
knows so every output from the claude code should be obvious - it should not be confusing like why is it like that.

One example is have a proper design system which everyone knows and create a skill for it and write a documentation file which should help agent know what is in your mind so it is not needed
for you to provide the details everytime - this is automated workflow rather than a assisted work. Build this kind of workflows - one better way to make agents produce better outputs is to 
have proper documentation for everything and direct the agents to required document in fresh sessions without burdening it with lot of or unwanted context which might result in better outputs.

Thing 1: Ensure you have proper understanding of what you are developing and ensure agent is inline with what you are thinking - this is one mistake i have done

One good way is to wriote the adr's which reflects the design decisions you have taken in the past and why have you taken it - so agent unnecesarily does not change anything and use that under-
standing to comeuop with similar decisions anytime in the future.

Ensure all the required skills are there and proper skills are there for every work. And proper documentation of every feature so it has a proper context of what we are doing and how we have done it \
so this is inline with us.

One more issue i have observed it does not follow the standard patterns like designing the api client(lib folder) which is responsible for making the calls to the backend apis to get the required 
data. It rather make the components to call the api directly from there - which is a leakage of logic. Its not obvious that api calls will be made from only api client - if it can be called from anywhere its confusing

Backend: 

Even in backend it sometimes make the same issues like below we need to resolve the issues. 

Example: if we want the controller - service - repository structure ensure this is followed everytime and not missed anytime. Due to the lack of this it is making the direct calls from the 
controller to the database which is actually not correct.

This is not secure way and also not obvious that all db calls will be made from certain place like repository.

Database:

Its better to define a skill that has the proper understanding of the db and how to connect to it and all as its very important for the agent to know how to get the required details as this is very 
important to automate the things when you want the agent to work independantly to work on a fix or test something is working with the real data.
